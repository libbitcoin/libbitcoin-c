Pure C wrapper for libbitcoin library.

Headers to access internal C++ objects are in `<bitcoin/bitcoin/c/internal/>`

Directory structure exactly mirrors libbitcoin, as do the method and type names.

